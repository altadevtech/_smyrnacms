# Configurações do Backend (Node.js)
NODE_ENV=development
PORT=10000
JWT_SECRET=seu_jwt_secret_super_seguro_aqui
DB_PATH=./server/smyrna.db

# Configurações do Frontend (Vite)
# Para sobrescrever a URL da API automaticamente detectada (opcional)
# VITE_API_URL=https://sua-api.render.com/api

# Para produção no Render, essas variáveis serão configuradas automaticamente:
# NODE_ENV=production
# PORT=10000
# JWT_SECRET=gerado_automaticamente_pelo_render
# DB_PATH=./server/smyrna.db

# Configurações SQLite para deploy (Render.com)
npm_config_build_from_source=true
npm_config_sqlite=/usr
PYTHON=/usr/bin/python3
